#include <bits/stdc++.h>
#define ll long long int
using namespace std;

class solve
{
public:
    solve()
    {
       int n;
       cin>>n;
       string s;
       cin>>s;
       sort(all(s));
       cout<<s;
    }
};
int main()
{

    int t;
    cin >> t;
    while (t--)
    {

        solve s;
    }
}