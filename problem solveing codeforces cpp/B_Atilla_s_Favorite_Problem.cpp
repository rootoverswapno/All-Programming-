#include <bits/stdc++.h>
#define ll long long int
using namespace std;

class solve
{
public:
    solve()
    {
       int n;
       cin>>n;
       string s;
       cin>>s; 
       sort(s.begin(),s.end());
       int l=s.back()-'a'+1;
       cout<<l<<endl;
    }
};
int main()
{

    int t;
    cin >> t;
    while (t--)
    {

        solve s;
    }
}